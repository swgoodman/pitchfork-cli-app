require 'spec_helper'

describe PitchforkCLI do
  it 'has a version number' do
    expect(PitchforkCLI::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
